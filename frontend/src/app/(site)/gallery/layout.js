export const metadata = {
  title: 'Gallery — Preva Kitchen',
  description: 'Explore the culinary artistry, signature dishes, and dining experience at Preva Kitchen in Redford Township, MI.',
  alternates: { canonical: '/gallery' }
};

export default function GalleryLayout({ children }) {
  return children;
}
