export const metadata = {
  title: 'Live Order Display Board — Preva Kitchen',
  robots: {
    index: false,
    follow: false,
    nocache: true
  }
};

export default function DisplayLayout({ children }) {
  return children;
}
