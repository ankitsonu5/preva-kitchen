export const metadata = {
  title: 'Chef KDS — Preva Kitchen',
  robots: {
    index: false,
    follow: false,
    nocache: true
  }
};

export default function KitchenLayout({ children }) {
  return children;
}
